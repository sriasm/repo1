lb = int (input('Enter the lowerband :'))
ub = int(input('Enter the upperband :'))
print (' The lowerband {0} is and the upperband is {1}'.format (lb,ub))
vif = str (input (' Can i Procced to check (y/n) '))
if vif == 'y' :
    print (' you pressed Y')
elif vif== 'n':
    print ('you pressed N')
else :
    print ('press y/n only')
list = [lb,ub]
print ('the inputed list :', list)

for i in range(lb, ub):
    data = i+1
    #print ('the value {0} of date is :'.format (i), data)
    list1.insert(data) = [list1]
    
    print ('List is :', list1)
    
